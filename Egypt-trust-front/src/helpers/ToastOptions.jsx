export const ToastOptions = (Type) => {

    if (Type === "success") {

        return {
            theme: "light",
        }
    } else if (Type === "error") {
        return {
            theme: "light",
        }
    }

}